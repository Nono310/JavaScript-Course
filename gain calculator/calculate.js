function calculateBill(bill){
    
    if (bill => 50){
        const gain = (bill * 15/100);
        console.log(gain());
    }else if (bill <= 300){
        const gain = (bill * 15/100);
        console.log(gain());

    }else {
        const gain = (bill * 20/100);
        console.log(gain());
    }

    const bill =[125, 555, 44];

}